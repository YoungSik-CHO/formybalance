export interface RadioGroupClasses {
    /** Styles applied to the root element. */
    root: string;
    /** Styles applied to the root element, if `row` is true. */
    row: string;
}
export declare type RadioGroupClassKey = keyof RadioGroupClasses;
export declare function getRadioGroupUtilityClass(slot: string): string;
declare const radioGroupClasses: RadioGroupClasses;
export default radioGroupClasses;
